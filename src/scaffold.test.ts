import { describe, expect, test } from 'vitest';

// 스캐폴딩 스모크 테스트. 실제 게임 로직이 들어오면 교체하세요.
describe('scaffold', () => {
  test('toolchain runs typescript under vitest', () => {
    const answer: number = 1 + 1;
    expect(answer).toBe(2);
  });
});
