/**
 * 포지션 시스템 기본 상수 — PRD §9.1 예측 파라미터.
 *
 * "전부 서버 컨피그로 관리한다. 클라이언트 하드코딩 금지"(§9. 밸런스 상수표 서두)라는
 * 원칙에 따라, 여기 값들은 어디까지나 **기본값**이며 실제 배포에서는 서버 컨피그가
 * 이 값을 덮어써야 한다. `sigma`는 스테이지(ChartSet)마다 달라지므로 여기 포함하지 않는다.
 */

import type { PositionParams } from './types';

/** `sigma`를 제외한 매매 파라미터 기본값. §9.1. */
export const DEFAULT_POSITION_PARAMS: Omit<PositionParams, 'sigma'> = {
  payoutBase: 0.9,
  zMax: 3.0,
  feeRate: 0.01,
  liquidationLine: 1.0,
  minHoldMs: 2000,
  maxPositions: 24,
};

/** 청산선의 이 비율에 도달하면 UI가 적색 경고로 전환된다 (FR-5.6). */
export const LIQ_WARN_RATIO = 0.8;

/**
 * 청산 대금(`원금 + 손익`)이 골드로 환산되는 비율 (`trade.ts` `closePosition`).
 *
 * ```
 * 골드 += floor(max(stake + pnl, 0) × GOLD_CONVERSION)
 * AUM 복귀 없음
 * ```
 *
 * ★ 왜 환급률(`REFUND_RATIO`)을 폐기했는가 ★
 * 이전 규칙은 "이익만 골드, 원금은 30% 태워 AUM 복귀"였다. 세탁(z≈0 왕복)은 막았지만
 * **개념이 어긋나 있었다** — 투자를 했으면 결과는 전부 골드가 되어야지, 원금 일부가
 * 다시 매매 재화로 돌아올 이유가 없다. 게다가 원금이 복귀하면 AUM이 재순환해
 * 세션 총 투입 S가 초기 AUM의 5~6배로 불어나고, 그 배수가 밸런스의 숨은 변수가 됐다.
 *
 * 지금은 AUM이 **일방통행 파이프**다. 세션 동안 굴릴 수 있는 총액은
 * `시작 AUM + AUM_DROP_PER_WAVE × 13`으로 못 박히고, 세탁을 막는 장치는 이 전환율 하나다.
 *
 * ★ 0.5인 근거 (임의값 아님) ★
 * R1의 두 앵커를 고정한 채 역산한 값이다.
 * - 앵커 1: R1 시작 AUM 2,000 (PRD §9.2 `AUM_BY_DESK_LV` Lv1. v1.0부터 불변)
 * - 앵커 2: `AUM_DROP_PER_WAVE` 150 (스킬 `S-03` 비용 150 AUM = 웨이브 1개분 드롭이라는
 *   환율을 유지해야 한다)
 * → R1 세션 총 투입 S = 2,000 + 150 × 13 = 3,950
 * → 총골드 = 315(시작 120 + 기본수입 195) + S × (1 + ρ) × GC
 * → R1 목표 ρ = 0.20에서 필요지출 2,700에 닿으려면
 *   GC = (2700 − 315) / (1.20 × 3950) = **0.5032** → 0.50으로 반올림(오차 0.6%).
 *
 * 이 값은 FR-8.1 잔여 AUM 계수(`AUM_SETTLEMENT_RATIO` = 0.15)와 한 쌍이다 —
 * **AUM은 의도적으로 나쁜 저장수단**이며, 쥐고 있으면 정산에서 15%밖에 못 건지고
 * 굴리면 전환에서 절반이 깎인다. 어느 쪽이든 "쌓아두기"가 답이 되지 않는다.
 */
export const GOLD_CONVERSION = 0.5;

/**
 * FR-8.1 정산에서 **잔여 AUM**을 자본금으로 환산할 때 쓰는 계수.
 *
 * v1.2의 0.5에서 내렸다. 계수가 높으면 **"아무 매매도 안 하고 AUM을 쥐고만 있으면
 * 전액 자본금"**이 되어 익스플로잇이 그쪽으로 이동한다. 0.15는 잔여 AUM을 "쓰지 않은
 * 실탄에 대한 소액 보상" 수준으로 눌러 그 경로를 죽인다.
 *
 * `GOLD_CONVERSION`(0.50)보다 낮게 유지해야 한다 — 그래야 "굴려서 골드로 바꾸는 쪽"이
 * "쥐고 정산받는 쪽"보다 항상 유리하고, AUM이 창고가 아니라 파이프로 남는다.
 */
export const AUM_SETTLEMENT_RATIO = 0.15;

/**
 * FR-8.1 `aumGate` 임계치 — 잔여 AUM을 자본금으로 환산받기 위한 **이익 청산(`pnl > 0`) 최소 횟수**.
 *
 * v1.2는 이 조건이 "총 **진입** 횟수 ≥ 4"였다. 진입은 아무 판단 없이도 4회 채울 수 있어서,
 * 세탁 4회면 게이트가 열려 **방지책이 오히려 익스플로잇 체크리스트**가 됐다.
 * 조건을 "이익 청산 횟수"로 바꾸면 z≈0 왕복으로는 절대 충족할 수 없다 — 게이트를 열려면
 * 실제로 차트를 읽고 이익을 내야 한다.
 */
export const MIN_PROFIT_CLOSES_FOR_AUM_CREDIT = 4;

/** 진입 UI에 노출되는 투입 비율 프리셋: 10% / 25% / 50% / 전액 (FR-5.1). */
export const STAKE_PRESETS: readonly number[] = [0.1, 0.25, 0.5, 1.0];
